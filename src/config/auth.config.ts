import type { NextAuthConfig } from "next-auth";

const authConfig: NextAuthConfig = {
  // TODO: Add your NextAuth configuration here
  // This should include providers, callbacks, etc.
  providers: [],
};

export default authConfig;
